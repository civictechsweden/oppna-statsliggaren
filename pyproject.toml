[project]
name = "oppna-statsliggaren"
version = "0.1.0"
description = "Open data for Swedish regleringsbrev"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "beautifulsoup4>=4.13.4",
    "markdownify>=1.1.0",
    "requests>=2.32.4",
    "requests-futures>=1.0.2",
]

[tool.setuptools]
packages = ["services"]